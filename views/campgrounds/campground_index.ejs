<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campgrounds</title>
</head>
<body>
    <% layout('boilerplate.ejs') %> 
    <h1>Campgrounds index page</h1>
    <a href="/campgrounds/new">add a new campground</a>
     <% for (let campground of campgrounds ) { %>
        <div class="card mb-3" style="max-width: 1240px;">
            <div class="row no-gutters ">
              <div class="col-md-4 ">
                <img class="img-thumbnail my-3" src="<%= campground.images[0].thumbnail %> " alt="...">
              </div>
              <div class="col-md-8">
                <div class="card-body ">
                  <h5 class="card-title"><%= campground.title %> </h5>
                  <p class="card-text"><small class="text-muted"><%= campground.location %> </small></p>
                  <p class="card-text"><%= campground.description %> </p>
                  <p class="card-text"><%= campground.price %> $ </p>
                  <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">Show info</a>
                </div>
              </div>
            </div>
          </div>
            
     <% }  %> 
    
</body>
</html>